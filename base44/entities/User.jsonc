{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "enum": [
        "admin",
        "user"
      ],
      "default": "user",
      "description": "User role: admin (full access), user (view data, reports, configure reporting, export)"
    }
  },
  "required": []
}